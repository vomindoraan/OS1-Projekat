#include "locks.h"
#include <iostream.h>

int userMain(int argc, char* argv[]);

int main(int argc, char* argv[]) {
	MASKED(cout << "Start" << endl);



	MASKED(cout << "Finish" << endl);
}
